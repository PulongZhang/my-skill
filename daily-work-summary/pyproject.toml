[project]
name = "daily-work-summary"
version = "0.1.0"
description = "Utilities for extracting daily Git activity and calculating diligent time"
requires-python = ">=3.8"
dependencies = []

[tool.uv]
package = false
