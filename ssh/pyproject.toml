[project]
name = "ssh-skill"
version = "3.3.1"
description = "SSH command, transfer, tunnel, and configuration utilities"
requires-python = ">=3.9"
dependencies = [
    "paramiko>=3.5,<4",
]

[tool.uv]
package = false
