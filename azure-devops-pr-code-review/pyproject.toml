[project]
name = "azure-devops-pr-code-review"
version = "0.1.0"
description = "Local tooling for Azure DevOps Server pull request reviews"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31,<3",
    "urllib3>=1.26,<3",
]

[tool.uv]
package = false
