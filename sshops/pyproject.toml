[project]
name = "sshops"
version = "3.6.0"
description = "SSH command, transfer, tunnel, and configuration utilities"
requires-python = ">=3.9"
dependencies = [
    "paramiko>=3.5,<4",
]

[tool.uv]
package = false
