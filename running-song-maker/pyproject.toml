[project]
name = "running-song-maker"
version = "0.1.0"
description = "Analyze songs, preserve vocal pitch, align tempo, and mix fixed-cadence running clicks"
requires-python = ">=3.11"
dependencies = [
    "librosa>=0.11,<1",
    "numpy>=1.26,<3",
    "scipy>=1.13,<2",
    "soundfile>=0.13,<1",
]

[tool.uv]
package = false
