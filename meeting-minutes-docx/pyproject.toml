[project]
name = "meeting-minutes-docx"
version = "0.1.0"
description = "Generate meeting minutes DOCX documents from structured input"
requires-python = ">=3.8"
dependencies = [
    "python-docx>=1.1,<2",
]

[tool.uv]
package = false
